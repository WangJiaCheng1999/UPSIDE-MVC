
//Average calculation
//values should be an arrary of number
function aveCal(values) {
    let total = 0;
    for (let i = 0; i < values.length; i++) {
        total += values[i];
    }
    return total / values.length;   

}

//Find the biggest number in values and return which Round it is.
//values should be an arrary of number
function findBiggestValue(values) {
    let biggestRound = "";
    for (let i = 0; i < values.length; i++) {
        total += values[i];
    }
}

//Median calculation
function medCal() {

}